backend:
  name: git-gateway
  branch: main

media_folder: "img/productos"
public_folder: "/img/productos"

collections:
  - name: "productos"
    label: "Productos"
    label_singular: "Producto"
    file: "js/productos.json" # <-- El CMS ahora editará este archivo
    extension: "json"
    format: "json"
    create: true
    fields:
      - label: "Lista de Productos"
        name: "lista_productos"
        widget: "list"
        fields:
          - {label: "ID", name: "id", widget: "number"}
          - {label: "Nombre", name: "nombre", widget: "string"}
          - {label: "Precio", name: "precio", widget: "number"}
          - {label: "Categoría", name: "categoria", widget: "select", options: ["Ropa", "Calzado", "Tecnología", "Hogar"]}
          - {label: "Descripción", name: "descripcion", widget: "text"}
          - label: "Imágenes"
            name: "imagenes"
            widget: "list"
            summary: "{{fields.url}}"
            fields:
              - {label: "Imagen", name: "url", widget: "image"}